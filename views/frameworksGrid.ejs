<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title>Frameworks</title>
</head>
<body>
    <% if(frameworks){%>
    <main class="main-grid">
        <% frameworks.forEach(frame =>{%>
            <a href="/framework/<%= frame.id%>">
                <div class="card">
                    <h2><%= frame.name %></h2>
                </div>
            </a>
        <%})%>
        <a class="btn-primary" href="/framework/new">+ New Framework</a>
    </main>
    <%}else{%>
        <h1>No frameworks yet.</h1>
    <%}%>
</body>
</html>